# Pinned-loose deps for scripts/convert_to_litertlm.py.
# Install into a venv before running the converter:
#   python3 -m venv ~/.venvs/litertlm-convert
#   source ~/.venvs/litertlm-convert/bin/activate
#   pip install -r scripts/convert_to_litertlm.requirements.txt
#
# These are heavy (multi-GB once torch + transformers land). The script runs
# CPU-only; a GPU is not required for conversion.

ai-edge-torch>=0.4
litert-lm-builder>=0.1
huggingface_hub>=0.25
transformers>=4.45
sentencepiece>=0.2
# ai-edge-torch's generative.layers.lora hard-imports
# tensorflow.lite.python.schema_py_generated but doesn't declare TF as a
# dep. Without this the conversion falls over with ModuleNotFoundError
# the first time the qwen/llama/etc converter loads.
tensorflow>=2.15
# torch comes in transitively from ai-edge-torch — let it pick a
# compatible version. Explicit pinning collides with the constraint
# graph (ai-edge-torch only supports Python 3.9-3.11; the venv is
# pinned to 3.11 in the Makefile to satisfy that).
